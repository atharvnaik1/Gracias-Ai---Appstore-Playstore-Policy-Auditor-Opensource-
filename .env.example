# API Keys (at least one required)
NVIDIA_KEY=     # NVIDIA NIM API key for ipaship provider
ANTHROPIC_API_KEY=  # Optional: Anthropic API key for Claude provider

# MongoDB (optional — for saving audit reports)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/ipaship

# Clerk (optional — for authentication)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
